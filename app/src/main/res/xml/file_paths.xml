<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Exported backup JSON lives in cacheDir/backups and is shared via FileProvider (Phase 23). -->
    <cache-path name="backups" path="backups/" />
</paths>
